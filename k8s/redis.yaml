apiVersion: apps/v1
kind: Deployment
metadata:
  name: oms-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oms-redis
  template:
    metadata:
      labels:
        app: oms-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: oms-redis
spec:
  ports:
    - port: 6379
  selector:
    app: oms-redis
